/* mongoose from "mongoose"
const userSchema =mongoose.Schema({

    Nom:{type:String, 
        require :true,
        enum : "sports","culture"},
        sousCategorie:{type:String}

export default  mongoose.model('Categorie',userSchema)*/
